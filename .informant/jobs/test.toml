name = "test"
command = """
set -euo pipefail
bun install --frozen-lockfile
bun test
"""
timeout_minutes = 15
environment = { CI = "true" }
cache = [{ paths = ["~/.bun/install/cache"], key_files = ["bun.lock"] }]
