name = "lint"
command = """
set -eu
bun run check
"""
timeout_minutes = 15
environment = { CI = "true" }
container.image = "oven/bun:1"
container.cpu = 2
container.memory_mb = 4096
container.prepareInputs = ["bun.lock", "bunfig.toml", "package.json", "apps/*/package.json"]
container.prepare = "bun install --frozen-lockfile --ignore-scripts"
