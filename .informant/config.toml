version = 1
timeout_minutes = 30
triggers = [
  { event = "commit", branch = { names = ["main"] } },
  { event = "commit", pull_request = { state = "open", draft = false } },
]

[container]
image = "oven/bun:1"
cpu = 2
memory_mb = 4096
